name: CI

on:
  pull_request:
  push:
    branches: [master, dev]

jobs:
  lint-skill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "lint-skill placeholder — Session 05"

  validate-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "validate-examples placeholder — Session 05"

  render-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "render-examples placeholder — Session 05"

  snapshot-counts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "snapshot-counts placeholder — Session 05"
